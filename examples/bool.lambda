True = (\t f. t)
False = (\t f. f)
and = (\a b. a b a)
or = (\a b. a a b)
not = (\a. a False True)
nand = (\a b. not (and a b))
nor = (\a b. not (or a b))
xor = (\a b. a (not b) b)
xnor = (\a b. a b (not b))
